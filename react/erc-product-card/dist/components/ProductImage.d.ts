import { CSSProperties } from 'react';
export interface Props {
    img?: string;
    customClassName?: string;
    customStyle?: CSSProperties;
}
export declare const ProductImage: ({ img, customClassName, customStyle }: Props) => JSX.Element;
